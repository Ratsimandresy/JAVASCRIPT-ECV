const bodyBgColor =
  document.getElementsByTagName("body")[0].style.backgroundColor;
const bodyWidth = document.getElementsByTagName("body")[0].style.width;

console.log(
  `The body's background-color is : ${bodyBgColor}, and the body's width is : ${bodyWidth}`
);
